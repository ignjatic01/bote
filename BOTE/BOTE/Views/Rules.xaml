<Window x:Class="BOTE.Views.Rules"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BOTE.Views"
        mc:Ignorable="d"
        Title="Rules" Height="650" Width="500"
         Background="#2D2D2D" Foreground="White">

    <Window.Resources>
        <Style TargetType="Window">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel>
            <!-- Naslov - Rules -->
            <TextBlock Text="Rules" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"/>
            <TextBlock Text="{Binding Rule1}" FontSize="18" Margin="20,20,20,20" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding Rule2}" FontSize="18" Margin="20, 20,20,20" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding Rule3}" FontSize="18" Margin="20,20,20,20" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding Rule4}" FontSize="18" Margin="20,20,20,20" TextWrapping="Wrap"/>
        </StackPanel>
    </ScrollViewer>
</Window>
